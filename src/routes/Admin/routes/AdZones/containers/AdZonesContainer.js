import { connect } from 'react-redux'

import AdZones from '../components/AdZones'

const mapStateToProps = (state) => ({
})

const mapActionCreators = {
}

export default connect(mapStateToProps, mapActionCreators)(AdZones)
